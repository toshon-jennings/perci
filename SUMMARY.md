# SUMMARY.md — Agent Orientation Cheat Sheet

> **PURPOSE:** This file exists to prevent unnecessary full-repo scans. AI
> agents MUST read this file first when they need orientation. It provides
> a map of the codebase so agents can jump directly to the relevant files
> instead of scanning every directory.

> **RULE:** Do NOT grep or glob the entire repo to find things. Use this
> document's pointers to navigate directly. Only search broadly when the
> answer isn't found via these pointers.

---

## Project

**Name:** Perci
**Description:** AI workspace desktop app — multi-provider chat, agent orchestration, deep research, code workbench, mission control.
**Target Platform:** Desktop (Electron) + Web (Vite dev server)
**Primary Language:** JavaScript (JSX)
**Framework:** React 18 + Electron 39

---

## Tech Stack

| Layer | Technology |
|-------|------------|
| Frontend | React 18, Tailwind CSS, Framer Motion |
| Backend | None (client-side app, shells out to OpenClaw/Hermes CLIs) |
| Database | Electron app-data file via `persistentStore.js` (encrypted, atomic writes) |
| Build | Vite 5 |
| Desktop Shell | Electron 39 |
| Testing | None (no test suite yet) |
| Styling | Tailwind CSS |

---

## Source Map

> **Convention:** `src/` is the source root. Paths below are relative to repo root (`/Users/toshonjennings/opal`).

### Entry Points
| File | Role |
|------|------|
| `src/main.jsx` | React DOM root — mounts `<App />` inside `<ModeProvider>` |
| `src/App.jsx` | Root component — window system, mode routing, global event wiring |
| `electron/main.cjs` | Electron main process — IPC bridge, OpenClaw integration |
| `electron/preload.cjs` | Electron preload — exposes `window.electron.*` to renderer |

### Core Directories
| Directory | Contains |
|-----------|----------|
| `src/components/` | 36 UI components (ChatMode, CodeMode, CoworkMode, MissionControl, etc.) |
| `src/context/` | 5 React contexts: ModeContext, ChatContext, BuildContext, BuildModeContext, ThemeContext |
| `src/lib/` | 18 modules: LLM clients, mission control, terminal bridge, security, search, etc. |
| `src/hooks/` | 2 custom hooks: useAgentTools, useWebContainer |
| `src/utils/` | 3 utilities: preview-generator, thinking-formatter, thinking-parser |
| `src/assets/` | Static assets (images, icons) |
| `electron/` | Electron main process, preload, splash screen |

### Cross-cutting Concerns
| Concern | Location |
|---------|----------|
| Mode/Window system | `src/context/ModeContext.jsx` — defines MODES enum, window open/close/focus |
| LLM provider clients | `src/lib/llm/clients.js` — factory pattern across 6+ providers |
| Mission control events | `src/lib/missionControl.js` — `appendMissionRunEvent()` central event bus |
| Terminal IPC | `src/lib/terminalBridge.js` — WebSocket bridge to terminal server |
| Artifact security | `src/lib/previewSecurity.js` + `src/lib/budgetGovernor.js` — sandboxed iframes, CSP |
| Persistence | `src/lib/persistentStore.js` — Electron app-data file (encrypted, atomic writes); localStorage web fallback |
| Theme | `src/context/ThemeContext.jsx` — light/dark/system theme switching |

---

## Key Architectural Patterns

1. **Window system (desktop metaphor):** The Dashboard (`MODES.DASHBOARD`) is the always-mounted "desktop." Every other mode (Chat, Code, Cowork, Mission, Build, Agents, etc.) opens as a floating window on top, surfaced by the bottom Dock. Windows are managed by `ModeContext` and persist across reloads via the Electron app-data file. See `src/components/windows/` for `DesktopHost`, `Dock`, `WindowFrame`.

2. **Mode routing:** `App.jsx` maps each `MODES` key to a component. Adding a new mode means: (a) add key to `MODES` in `ModeContext.jsx`, (b) add component import + route in `App.jsx`, (c) add window title in `WINDOW_TITLES`. Non-mode windows (OpenClaw, Hermes, YouTube) use the same window system but are registered separately with `OPENCLAW_WINDOW_ID`, etc.

3. **Electron IPC bridge:** `electron/preload.cjs` exposes `window.electron.*` methods to the renderer. `electron/main.cjs` handles these via IPC handlers. **Critical:** The preload is loaded once at startup — changes require a full Electron restart. The root `main.cjs` is a dead legacy file — do NOT edit it.

4. **Knowledge graph:** `docs/architecture/graphify-out/` contains a queryable graph of the renderer codebase (564 nodes, 37 communities). Use `graphify query` for structural relationships before diving into source files.

---

## What NOT to Touch

| Path | Why |
|------|-----|
| `main.cjs` (root, not `electron/`) | Legacy orphan — not loaded by the app |
| `src/App.jsx.backup` | Backup file — ignore |
| `dist/`, `dist_electron/`, `build/` | Build artifacts — regenerated |
| `node_modules/` | Dependencies — don't edit directly |
| `graphify-out/` | Generated by graphify tool |

---

## Common Gotchas

1. **Electron restart required** — Changing `electron/preload.cjs` or `electron/main.cjs` requires a full Electron app restart. The preload bridge is loaded at startup; IPC methods that appear to exist but silently fail mean the old preload is still in memory.

2. **React compiler memo cache** — Adding deps to memoized blocks shifts ALL subsequent `$[N]` indices. Use Python replacements, never hand-edit. Verify with `grep -n '\$\['` after.

3. **App-data state hydration** — `ModeContext.jsx` hydrates window state, code state, chat state, and OpenClaw config from the Electron app-data file on mount via `persistentStore.ensureHydrated()`. If state shape changes between versions, add a normalization function (see `hydrateWindows`, `normalizeCodeState`).

4. **Artifact iframe security** — AI-generated code previews run in sandboxed iframes with CSP. `src/lib/previewSecurity.js` enforces this. Don't relax CSP without understanding the implications.

5. **OpenClaw IPC vs plugin** — The `copilot-proxy` plugin is NOT the ACP runtime. You must also `openclaw plugins install @openclaw/acps` + enable it + gateway restart for ACP harnesses.

---

## How to Navigate by Task

| If you need to... | Go to... |
|--------------------|------------------|
| Change chat UI | `src/components/ChatMode.jsx` + `src/components/ChatMessage.jsx` |
| Add a new mode/window | `src/context/ModeContext.jsx` (add MODES key) + `src/App.jsx` (add route) |
| Modify window chrome | `src/components/windows/WindowFrame.jsx` + `src/components/windows/Dock.jsx` |
| Change LLM provider logic | `src/lib/llm/clients.js` + `src/lib/llm/ModelService.js` |
| Add IPC method | `electron/preload.cjs` (expose) + `electron/main.cjs` (handle) |
| Change mission control | `src/lib/missionControl.js` + `src/components/MissionControl.jsx` |
| Modify terminal bridge | `src/lib/terminalBridge.js` + `src/components/Terminal.jsx` |
| Change settings/API keys | `src/components/SettingsModal.jsx` |
| Theme/styling changes | `src/context/ThemeContext.jsx` + `src/index.css` (Tailwind) |
| Artifact preview | `src/components/ArtifactPanel.jsx` + `src/components/LivePreviewPanel.jsx` |
| Deep research | `src/lib/IntelligentSearchTool.js` + `src/components/AutoresearchPanel.jsx` |
| Agent orchestration | `src/components/AgentsPanel.jsx` + `src/components/CoworkMode.jsx` |
